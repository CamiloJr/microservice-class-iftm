spring.application.name=eureka-cs-feign

# apontando para o servidor de configuracoes:
spring.config.import=optional:configserver:http://config-server-container:8888
management.endpoints.web.exposure.include=*

# Todas as configs abaixo, agora estao no github
#server.port=${PORT:0}
#eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka